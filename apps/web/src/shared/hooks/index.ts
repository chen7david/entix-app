/**
 * Shared Hooks
 * Reusable hooks used across different features
 */

// Re-export existing hooks
export * from '../../hooks/auth.hook';
export * from '../../hooks/useMediaQuery';

// Profile hooks (already refactored)
export * from '../../hooks/profile';
